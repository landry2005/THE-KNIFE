# Indirizzo del server database (locale)
db.host=localhost
# Porta standard di PostgreSQL
db.port=5432
# Nome del database appena creato
db.name=theknifedb
# Utente predefinito di PostgreSQL
db.user=postgres
# LA TUA PASSWORD di PostgreSQL (non committare questo file su Git!)
db.password=5555